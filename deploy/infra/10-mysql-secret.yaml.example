# 复制为 10-mysql-secret.yaml（已被 .gitignore 忽略），
# 改一个自己的密码，只在 master 上编辑。
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: kaiops
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "CHANGE-ME"
