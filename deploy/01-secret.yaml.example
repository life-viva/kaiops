# 复制为 01-secret.yaml（已被 .gitignore 忽略，不会进 git），
# 把 LLM_API_KEY 换成你自己的 DeepSeek key，只在 master 上编辑。
apiVersion: v1
kind: Secret
metadata:
  name: llm-secret
  namespace: kaiops
type: Opaque
stringData:
  LLM_API_KEY: "sk-REPLACE-ME"
